name: Build
on:
  workflow_call:
    inputs:
      branch:
        description: PDFium branch
        type: string
        required: false
        default: main
      version:
        description: PDFium version
        type: string
        required: false
        default: ""
      enable_v8:
        description: Enable V8
        type: boolean
        required: false
        default: false
      is_debug:
        description: Is debug
        type: boolean
        required: false
        default: false
      target_os:
        description: Target OS (android|emscripten|ios|linux|mac|wasi|win)
        type: string
        required: true
      target_cpu:
        description: Target CPU (arm|arm64|ppc64|x64|x86|wasm)
        type: string
        required: true
      target_environment:
        description: Target environment (device|catalyst|musl|simulator)
        type: string
        required: false
        default: ""
      emsdk_version:
        description: Emscripten SDK version
        type: string
        required: false
        default: 3.1.72
      wasi_sdk_version:
        description: WASI SDK version
        type: string
        required: false
        default: "24"
    outputs:
      artifact:
        description: "The name of the artifact"
        value: ${{ jobs.build.outputs.artifact }}

permissions:
  contents: read
  actions: write

jobs:
  build:
    outputs:
      artifact: ${{ steps.init.outputs.artifact }}
    runs-on: ${{ (inputs.target_os == 'ios' || inputs.target_os == 'mac') && 'macos-15' || inputs.target_os == 'win' && 'windows-2022' || 'ubuntu-24.04' }}
    env:
      PDFium_BRANCH: ${{ inputs.branch }}
      PDFium_IS_DEBUG: ${{ inputs.is_debug }}
      PDFium_TARGET_CPU: ${{ inputs.target_cpu }}
      PDFium_TARGET_OS: ${{ inputs.target_os }}
      PDFium_TARGET_ENVIRONMENT: ${{ inputs.target_environment }}
      PDFium_ENABLE_V8: ${{ inputs.enable_v8 }}
      PDFium_VERSION: ${{ inputs.version }}
      EMSDK_VERSION: ${{ inputs.emsdk_version }}
      WASI_SDK_VERSION: ${{ inputs.wasi_sdk_version }}
      MUSL_URL: ${{ secrets.MUSL_URL }}

    steps:
      - name: Initialize
        id: init
        shell: bash
        run: |
          ARTIFACT="pdfium"
          [ "$PDFium_ENABLE_V8" == "true" ] && ARTIFACT="$ARTIFACT-v8"
          # Both emscripten and wasi are wasm targets; omit the OS segment for both
          # so artifact names stay consistent (e.g. pdfium-wasm, pdfium-v8-wasm)
          if [ "$PDFium_TARGET_OS" != "emscripten" ] && [ "$PDFium_TARGET_OS" != "wasi" ]; then
            ARTIFACT="$ARTIFACT-$PDFium_TARGET_OS"
          else
            # Distinguish emscripten vs wasi builds with a suffix before the cpu
            ARTIFACT="$ARTIFACT-$PDFium_TARGET_OS"
          fi
          [ "$PDFium_TARGET_ENVIRONMENT" != "" ] && ARTIFACT="$ARTIFACT-$PDFium_TARGET_ENVIRONMENT"
          ARTIFACT="$ARTIFACT-$PDFium_TARGET_CPU"
          [ "${{ inputs.is_debug }}" == "true" ] && ARTIFACT="$ARTIFACT-debug"
          echo "artifact=$ARTIFACT" >> $GITHUB_OUTPUT

      - name: Checkout this repo
        uses: actions/checkout@v6

      - name: Set environment variables
        run: steps/00-environment.sh
        shell: bash

      - name: Install (1/2)
        run: steps/01-install.sh
        shell: bash

      - name: Checkout PDFium
        run: steps/02-checkout.sh
        shell: bash
        timeout-minutes: 15

      - name: Apply patches
        run: steps/03-patch.sh
        shell: bash

      - name: Install Emscripten SDK
        if: inputs.target_os == 'emscripten'
        run: steps/04-install-emsdk.sh
        shell: bash

      - name: Install WASI SDK
        if: inputs.target_os == 'wasi'
        run: steps/04-install-wasi-sdk.sh
        shell: bash

      - name: Install (2/2)
        # Skip for wasm targets that have their own dedicated install steps above;
        # only run for non-wasm targets (or keep and guard inside the script itself)
        if: inputs.target_os != 'emscripten' && inputs.target_os != 'wasi'
        run: steps/04-install-extras.sh
        shell: bash

      - name: Configure
        run: |
          steps/05-configure.sh
          cat <<END >>$GITHUB_STEP_SUMMARY
          ## args.gn
          \`\`\`gn
          $(cat pdfium/out/args.gn)
          \`\`\`
          END
        shell: bash

      - name: Build
        run: steps/06-build.sh
        shell: bash

      - name: Stage
        run: steps/07-stage.sh
        shell: bash

      - name: Collect licenses
        run: steps/08-licenses.sh
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ steps.init.outputs.artifact }}
          path: staging/

      - name: Test
        run: steps/09-test.sh
        shell: bash
